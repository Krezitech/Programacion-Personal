const producto = {
    nombre: 'Monitor de 24"',
    precio: 1900,
    disponibilidad: true
}

console.log(producto);

// Para acceder a los valores, se usa ".", pero tmb []

console.log(producto.nombre);
console.log(producto['nombre']);

console.log(producto.precio);
console.log(producto.disponibilidad);