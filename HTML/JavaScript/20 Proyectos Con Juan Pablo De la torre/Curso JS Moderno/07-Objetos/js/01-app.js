// En vez de agregar cada variable por separado, se pueden usar los objetos

const nombre = 'Monitor de 20"';
const precio = 1200;
const disponible = true;

// Se puede hacer como un objeto (object literal) de la siguiente manera: 

const producto = {
    nombre: 'Monitor de 24"',
    precio: 1900,
    disponibilidad: true
}

console.log(producto);