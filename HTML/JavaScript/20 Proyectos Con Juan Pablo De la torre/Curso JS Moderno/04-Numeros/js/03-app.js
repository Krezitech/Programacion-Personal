let resultado;

// Pi
resultado = Math.PI;

// Redondear
resultado = Math.round(4.49999999999);
resultado = Math.round(4.5);

// Redondear hacia arriba
resultado = Math.ceil(2.1);      // Dará 3

// Redondear hacia abajo
resultado = Math.floor(2.99);    // Dará 2

// Raíz Cuadrada
resultado = Math.sqrt(121);

// Valor Absoluto
resultado = Math.abs(-31);

// Potencia
resultado = Math.pow(2,5);

// Buscar el Mínimo
resultado = Math.min(0,-6,2,3,1,-3,-5,-2);      // -6

// Buscar el Máximo
resultado = Math.max(0,-6,2,3,1,-3,-5,-2);      // 3

// Número Aleatorio
resultado = Math.random();

// Número Aleatorio dentro de un rango
resultado = Math.floor(Math.random()*3)+1;


console.log(resultado);

