// Se inicializan las variables con un valor

var producto = "Monitor de 24'";
console.log(producto);

// Las variables se pueden reasignar

producto = "Monitor de 29";
console.log(producto);

// Las variables son dinámicas, no especifíca tipo de dato3

producto = 20;
console.log(producto);

// Se pueden inicializaar sin valor y agregarlo después

var disponible;
disponible = true;

disponible = false;

// Inicializar múltiples variables (se pone "var" al inicio, y luego ",", salvo la última, esa lleva ";")

var categoria = "Computadoras",
  marca = "Marca Famosa",
  calificacion = 5;

// Las variables NO pueden iniciar con números

// Las variables con mas de una palabra es poner apartir de segunda palabra con Mayúscula la primera letra (camel case)
//                                                                   o se pueden usar guiones bajo (underscore o snake)
//                                                            Las Clases se ponen en mayúscula la primera (Pascal case)
